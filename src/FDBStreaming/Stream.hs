module FDBStreaming.Stream (
  Stream,
  StreamName,
  isStreamWatermarked,
  getStreamWatermark,
  customStream
) where

import FDBStreaming.Stream.Internal
  (
    Stream,
    StreamName,
    isStreamWatermarked,
    getStreamWatermark,
    customStream
  )
